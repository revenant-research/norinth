# Development & CI tooling (pinned to majors for reproducibility).
# CI installs the hash-pinned requirements-dev.lock.txt generated from this
# file. Run `make lock-ci` after changing it.
-r apps/platform/requirements.txt
pytest>=9.1.1,<10
pytest-asyncio>=1.4.0
ruff>=0.16.4,<1.0
mypy>=2.3.1,<2.5
httpx>=0.28.1
pytest-cov>=7.1.0,<8
