apiVersion: v1
kind: Service
metadata:
  name: {{ include "norinth.fullname" . }}
  labels: {{- include "norinth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector: {{- include "norinth.selectorLabels" . | nindent 4 }}
