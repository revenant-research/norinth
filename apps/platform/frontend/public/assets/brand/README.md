Brand streak images used as landing-page chrome (Norinth is a Revenant Research product). Drop the four abstract
images here as:

  streak-1.jpg   indigo → magenta → amber diagonal streaks
  streak-2.jpg   indigo → violet → magenta gradient
  streak-3.jpg   teal → pink → violet fibre streaks
  streak-4.jpg   red → amber → green fibre streaks

Recommended: 1800px wide JPEG, quality ~80 (sips -s format jpeg -s formatOptions 80 --resampleWidth 1800 in.png --out streak-1.jpg).
If a file is missing the page falls back to a CSS gradient in the same palette.
